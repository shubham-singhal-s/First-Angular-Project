import { DexDirective } from './dex.directive';

describe('DexDirective', () => {
  it('should create an instance', () => {
    const directive = new DexDirective();
    expect(directive).toBeTruthy();
  });
});
